export type Url = string | `http://${string}` | `https://${string}`;

export type Iso8601 = `${number}-${number}-${number}T${number}:${number}:${number}Z`;
export type Iso8601Discord = `${number}-${number}-${number}T${number}:${number}:${number}.${number}Z`;
